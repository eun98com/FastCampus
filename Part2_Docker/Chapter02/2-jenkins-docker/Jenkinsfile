def mainDir="Part2_Docker/Chapter02/2-jenkins-docker"

pipeline {
    agent any

    environment{
        docker_hub_credentals = credentials('docker-hub-credentials')
        docker_image_name = 'eun98com/jib-test'
        docker_image_tag = "${env.BUILD_NUMBER}"
    }

    stages {
        stage('Pull Codes from Github'){
            steps{
                checkout scm
            }
        }
        stage('Build Codes by Gradle') {
            steps {
                sh """
                cd ${mainDir}
                ./gradlew jibDockerBuild -Djib.to.image=${docker_image_name}:${docker_image_tag}
                """
            }
        }
        stage('Push to Docker-hub Repository') {
            steps {
                    script {
                        docker.withRegistry('', docker_hub_credentals){
                            sh """
                            cd ${mainDir}
                            """
                            def customImage = docker.image("${docker_image_name}:${docker_image_tag}")
                        customImage.push()
                        }
                }
            }
        }
        stage('Deploy to Server'){
            steps{
                sshagent(credentials : ["deploy-key"]) {
                    sh "ssh -o StrictHostKeyChecking=no nuc@10.32.239.42 \
                      docker run -d -p 80:8080 -t ${docker_image_name}:${docker_image_tag};'"
                }
            }
        }
    }
}
